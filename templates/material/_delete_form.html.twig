<form method="post"
      id="frmDelete"
      action="{{ path('material_delete', {'id': material.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ material.id) }}">
</form>